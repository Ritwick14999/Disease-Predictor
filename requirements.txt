# Runtime dependencies for the full application (training + API + UI).
# The package itself declares finer-grained extras in pyproject.toml; this file
# is the one-shot install for `pip install -r requirements.txt`.
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3
tabulate>=0.9
xgboost>=2.0
imbalanced-learn>=0.11
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
streamlit>=1.30
matplotlib>=3.7
